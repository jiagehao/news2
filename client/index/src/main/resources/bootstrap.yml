spring:
  cloud:
    config:
      name: index,eureka,feign,database,redis,key
      label: master
      discovery:
        enabled: true
        serviceId: config
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: yewa
    password: 251212
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
